#
# Copy this file to `.env` and fill in your own API keys.
# This file is intentionally committed; `.env` is ignored.
#

# Required for local access to MCP Tools
# You can keep this dev value locally; change if you want.
MCP_API_KEY=dev-key

# Required for orchestrator classification/summarization
# Get from Google AI Studio
GEMINI_API_KEY=

# Optional but recommended: some deployments require a key
# Get from https://exchangerate.host (or your provider)
EXCHANGERATE_API_KEY=

# Required for OpenAQ v3
# Get from https://docs.openaq.org/docs/authentication
OPENAQ_API_KEY=

# Optional overrides (usually leave as defaults)
# GEMINI_MODEL=gemini-2.5-flash-preview-09-2025
# CLASSIFICATION_DEFAULT_DATE=2025-11-10
# HTTP_TIMEOUT_SEC=15
# NEARBY_RADIUS_M=3000
# NEARBY_LIMIT=1
# ROUTE_PROFILE_DEFAULT=foot
# ORCHESTRATOR_URL=http://localhost:3002